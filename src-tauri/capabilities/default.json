{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the monitor window.",
  "windows": ["main", "monitor-overlay"],
  "permissions": ["core:default"]
}
